# lib/ —— 通用工具层

**只负责**：与游戏无关的纯函数工具（格式化、clamp、防抖等）。

**绝不允许**：出现任何游戏概念（卡牌/稀有度/战斗…，那些属于 `engine/`）；import `vue` / `pinia` / `@/stores`（Vue 相关的可复用逻辑放 `composables/`）。

判断口诀：这个函数如果复制到一个记账 App 里也能用，就放 lib；否则它不属于这里。
