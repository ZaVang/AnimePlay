# 前端构建期环境变量示例。复制为 .env（或在 CI / 部署脚本里注入）后生效。
# 这些值在 `npm run build` 时烘焙进产物，改值需重新构建。

# 图片资源源（图片 offload 开关）。
# - 留空 / 不设：图片走同源 /data/images/...，由 VPS/Flask 本地托管（香港单机、本地 dev 用这个，行为不变）。
# - 设为图床域名：图片改从 OSS/CDN 直取，浏览器绕过 VPS 直连，VPS 带宽不被图片占用
#   （大陆便宜机 + 阿里云 OSS 的部署形态）。务必是 https 绝对地址、不带末尾斜杠。
# VITE_IMAGE_BASE_URL=https://img.example.com
