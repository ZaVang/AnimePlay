# "动画宅的自我修养" V2.0 开发计划与时间表

> 进度状态更新于 **2026-06-12**（依据实际代码核对，非原始勾选）。
> 标注说明：`[x]` 已完成 · `[~]` 已实现但存在缺陷/未闭环 · `[ ]` 未开始。
> "已实现但有缺陷"的具体证据见 `docs/项目审计报告-2026-06-12.md`。

## Step1: 前端重构并添加新 features
- [x] 主体功能（除战斗系统外）重构
- [x] 抽卡界面增加 UP 池、概率说明、商店和抽卡记录
- [x] 收藏界面展示更详细信息
- [x] 允许用户收藏喜爱的卡牌，并在主页展示
- [x] 收藏界面卡牌按稀有度排序 + 按稀有度/tag 筛选
- [x] 为不同稀有度卡牌设计不同特效
- [x] 将特效应用到抽卡界面
- [x] 收藏界面显示收藏统计预览
- [x] 收藏界面和卡牌详情显示分解选项
- [x] 卡组界面优化（右侧缩略图、左侧已入组高亮）
- [x] 观看队列按稀有度排序（`AddToQueueModal` 已按稀有度降序）
- [x] 已观看番剧不能再看（`AddToQueueModal` 过滤 `watchedAnime`）

## Step2: 重新设计战斗系统（宅理论战）
- [x] 写好战斗文档，确定基础逻辑（见 `战斗系统.md`）
- [x] 完善战斗逻辑（回合流程、出牌/防御、技能钩子、复杂交互）
- [x] 设计数值（`RewardCalculator.ts` 4 张结果表已落地）
- [x] 设计 AI（3 套主题档案 + `random-ai` 智能生成对手）
- [~] **对局结算未闭环**：`TurnManager.endGame()` 是空壳，打完一局无胜负界面、无奖励发放 → 待修
- [~] **技能效果约 78% 为假实现**（112 处 TODO）→ 待重构为参数化原语
- [ ] AI 不会使用角色技能（`AIController.ts` TODO）

## Step3: 部署上线
- [ ] 重构后端 API，配置文件也通过后端获取（当前配置仍全在前端 `gameConfig.ts`）
- [~] 部署方案：`api/index.py` 已备 Vercel serverless 版用户接口（/tmp 存储，未正式部署）
- [ ] 部署并开放内测
- [ ] **前置安全债**：存档接口无鉴权、`debug=True` —— 上线前必须处理（见审计报告五.2）

## Step4: 其他模块开发
- [~] 战斗界面特效优化（`DialogueSystem`/`BattleSpeechBubble`/`BattleActionEffect` 已做；音频未做）
- [x] UP 卡池定期轮换（`gachaRotation.ts` 按日期轮换 UR/HR，含缓存）
- [~] 额外的角色养成系统（`NurtureView` + 6 组件已做；训练冷却不持久化、部分互动是 TODO，见 `角色养成系统.md`）
- [~] 额外的角色战斗系统（`SquadBattleView` 爬塔已做；**小队/塔进度不入存档，刷新归零** → 待修）
- [ ] UR 角色自带 BGM（音频系统整体"开发中"）

## 计划外已上线（原计划未列，2026-04 新增）
- [x] **猜角色游戏**（`GuessView`）：像素化 4 难度档猜角色，稀有度加权计分（见 `猜角色游戏.md`）
- [x] **全局主题系统**（`stores/theme.ts`）：6 套配色，CSS 变量驱动全站换肤（见 `主题系统.md`）
  - [~] 遗留：组件大量硬编码 `text-white`（172 处），未接入主题文字色变量 → 导致浅色主题下标题泛白

## 当前最高优先级待办（来自 2026-06 审计）
1. 修复 172 处 `text-white` → 主题变量（视觉收益最大）
2. 宅理论战补结算 + 奖励发放
3. 小队/爬塔进度纳入存档协议；登出按钮绑定事件
4. 后端剥离 `all_animes` 的 `main_characters` 字段（首屏 7.2MB → ~0.5MB）+ gzip + 恢复图片缓存
5. 存档接口加鉴权、关 `debug=True`

> 完整路线图（三梯队）见 `docs/项目审计报告-2026-06-12.md` 第七节。
