# 抽卡系统

> 本文基于实际代码（`config/gameConfig.ts`、`stores/gachaStore.ts`、`utils/gachaRotation.ts`）编写，取代旧的《抽卡机制》《README_CHARACTER_SYSTEM》《角色等级划分说明》三份已废弃文档。
> 动画卡池与角色卡池机制完全对称，下文以统一口径描述。

## 稀有度与概率

稀有度共 6 级，概率由**权重归一化**得出（`gachaStore.ts:40-71`），不是直接的百分比。

| 稀有度 | 权重 `p` | 实际概率 | 分解价值（知识点） |
|---|---|---|---|
| UR  | 0.5 | 0.83% | 200 |
| HR  | 2.5 | 4.17% | 100 |
| SSR | 7   | 11.67% | 50 |
| SR  | 20  | 33.33% | 25 |
| R   | 30  | 50.00% | 10 |
| N   | 0   | 0%（不参与常规抽取） | 3 |

- 有效总权重 = 0.5 + 2.5 + 7 + 20 + 30 = **60**；各档概率 = `p / 60`。
- **N 卡 `p=0`，正常抽卡抽不到**，仅在某稀有度池为空的兜底分支才可能出现（`gachaStore.ts:90-92`）。
- 数值来源：`gameConfig.ts` 的 `animeSystem.rarityConfig` / `characterSystem.rarityConfig`（两者数值相同）。

> 稀有度由**后端**离线分级、冻结进数据文件的 `rarity` 字段；前端不计算稀有度，只消费已分级的卡数据。

> **评级标准（2026-06 改为「品质×人气」综合，权威步骤 = `backend/regrade_rarity.py`）**：
> - **番剧** composite = `0.65·中位百分位(rating_score) + 0.35·中位百分位(rating_total)`——口碑略重、人气次之；全 647 番**统一**排名分档（消除旧版「人气前250入桶→桶内评分排名→小众一刀切 SR」造成的跨桶倒挂）。权重在 `regrade_rarity.py` 的 `W_QUALITY` 一处可调。
> - **角色** 无 bangumi 评分，按 **Bangumi 角色人气分 `popularity_score`** 全局排名（并列用 collects/id 兜底）。**不**用 collects+番加成评级——那会让小角色蹭名作（个人人气低却进高档）；`comprehensive_popularity` 仅作卡详情「综合人气」展示。
> - 分档**保持各稀有度当前数量**（UR28/HR43/SSR58/SR115/R259/N144 番；角色同理），故抽卡概率盘面/图鉴规模不变，只是「谁该是什么档」重排。脚本只改 `rarity`（角色另改 `comprehensive_popularity`），其余字段与顺序原样；稀有度不进存档、不影响对战强度，安全可回滚。
> - 数据管线顺序：`create_curated_dataset.py`（头部策展）→ `expand_niche_dataset.py`（增量小众）→ **`regrade_rarity.py`（统一重排，最终权威）**。前两步内的稀有度赋值会被 regrade 覆盖。

> **数据集规模（2026-06）**：精选卡池 = 647 番剧（头部 289 全量策展 + 尾部 358 部 rating_total∈[1000,5000) 的小众番增量）+ 1454 角色（766 + 688，每部小众番≤3 角色）。`data/selected_*/all_cards.json`。前端 `gameDataStore` 用 `?limit=100000` 一次拉全（后端默认仅切 50）。

## UP 池（限时轮换）

- 每个卡池每日轮换 **1 张 UR + 1 张 HR** 作为 UP 卡（`utils/gachaRotation.ts`，按日期确定性计算，每日 0 点切换，带多级缓存）。
- **UP 命中规则**（`gachaStore.ts:73-83`）：当本次抽卡 roll 到 UR 或 HR 稀有度时，有 **66%** 概率（`rateUp.hrChance = 0.66`）将结果替换为对应稀有度的 UP 卡。
- UP 池配置：`gameConfig.ts` 的 `rateUp.hrChance`。`rateUp.ids` 为空数组，运行时由轮换系统动态填充。

## 保底机制

两套独立保底，状态存在 `userStore` 的 `animePityState` / `characterPityState`（字段：`totalPulls`、`pullsSinceLastHR`）。

1. **UP 保底**（`gachaStore.ts:57-59`）：`pullsSinceLastHR` 达到 **70** 抽仍未获得 UP 卡时，第 70 抽**必定**给出 UP 卡。配置：`rateUp.pityPulls = 70`。
2. **十连保底**（`gachaStore.ts:105-112`）：单次抽卡数 ≥ **10** 且整组没有任何 SSR/HR/UR 时，将其中一张 N/R/SR 替换为随机 SSR。配置：`gacha.guaranteedSSR_Pulls = 10`。
3. **pity 重置时机**：抽到 UP 卡、或抽到 HR/UR 稀有度时，`pullsSinceLastHR` 归零。

## 经济与资源

| 项 | 值 | 来源 |
|---|---|---|
| 初始动画券 / 角色券 | 100 / 100 | `gameConfig.ts:127-128` |
| 抽卡获得经验（动画） | 单抽 10 / 十连 110 | `gameplay.animeGachaEXP` |
| 抽卡获得经验（角色） | 单抽 15 / 十连 160 | `gameplay.characterGachaEXP` |
| 重复卡分解 | 见上稀有度表（→ 知识点） | `rarityConfig.dismantleValue` |

## 商店（知识点兑换道具）

`components/gacha/GachaShop.vue` —— 只售**非卡牌道具**：抽卡券、经验药水、知识点包（`getRegularShopItems` + `userStore.purchaseShopItem`，每日限购经 `shop.canPurchase` 真实计数）。

> **知识点→卡牌只有一条路**：商店不再直购任何卡牌。想用知识点定向拿某张卡，走「卡牌收藏 → 图鉴 → 点灰位卡解锁」（`CodexPanel` + `userStore.unlockCodexCard`，定价见 `config/codexUnlock.ts`）。
>
> 历史沿革：早期商店有「今日 UP / 历史 UP」两个页签也用知识点直购 UP 卡（`getCurrentUpShopItems` 1500/800、`getHistoricalUpShopItems` 1800/960），与图鉴定向解锁（UR 12000…）形成双重入口且价差矛盾——已删除，统一收口到图鉴解锁。

## 抽卡流程（`GachaView.vue`）

1. 选择动画 / 角色卡池。
2. 卡池页：查看 UP Banner（含轮换倒计时、保底说明）、单抽 / 十连、概率一览弹窗。
3. 执行抽卡 → `gachaStore.performGachaLogic(type, count)` 返回结果（标 `isNew` / `isDuplicate`）→ `GachaResultModal` 展示 → 入库到 `userStore` 收藏 + 结算经验。

## 关键文件

- `config/gameConfig.ts` —— 概率/保底/分解/经验全部配置（**调平衡改这里**）
- `stores/gachaStore.ts` —— 抽卡核心逻辑（`performGachaLogic`）
- `utils/gachaRotation.ts` —— 每日 UP 轮换 + 缓存
- `stores/userStore.ts` —— 券/知识点钱包、pity 状态、收藏入库、商店购买
- `components/gacha/` —— `GachaView` / `UpBanner` / `GachaShop` / `GachaHistory` / `GachaResultModal` / `GachaRatesModal`

## 架构提示

抽卡规则在 `engine/gacha/draw.ts`，随机源由调用方注入；`gachaStore` 持有保底与抽卡历史并负责持久化装配，`userStore` 只保留兼容门面和跨域编排。结果目前仍由客户端生成，单机使用无碍；若接入排行榜或多人玩法，抽卡与保底必须迁移到服务端权威结算，否则客户端结果可被篡改。

---
*创建于 2026-06-12，基于实际代码核对。*

## 商店每日限购（S6 起真实生效）

带 `dailyLimit` 的商店物品按「本地日期 + 物品 id」真实计数（`stores/shop.ts`，进存档 v3 `shopPurchases`），跨天自动重置；售罄后按钮禁用并提示「今日已售罄」。
