# AnimePlay — SPRINT 合同

> 本文件是 **product-loop / multi-ralph 的执行合同**：迭代引擎读这里的 `[ ]` 任务当需求源，
> 完成一项就把 `[ ]` 改 `[x]`，全部勾完且「验收命令」全过 → `DECISION: COMPLETE`。
> 单一任务源仍是 [FUTURE.md](FUTURE.md)（S0–S9 已 ✅）；本文件是把 FUTURE.md **剩余任务**（S10/S11/S12）
> 拆成可执行合同。当前活动 sprint = **S10**；S11/S12 见文末 Backlog（决策门控，未激活）。

## 产品背景

- **产品名称**：动画宅的自我修养（AnimePlay）
- **简介**：基于 Bangumi 番剧/角色数据的抽卡 + 收集 + 多玩法（宅理论战/挑战塔/养成/猜角色）网页游戏。全部游戏逻辑在前端（已抽成纯净 `engine/`），后端是「带文件柜的数据服务器」。
- **技术栈**：前端 Vue 3 + TS + Pinia + Tailwind（Vite）；后端 Flask（`backend/server.py`，另有 `api/index.py` 为 Vercel serverless 变体）。
- **启动方式**：后端 `python start_server.py`（端口 **5001**）；前端 `cd frontend-vue && npm run dev`（端口 **5173**，vite 代理 `/api`、`/data` 到 5001）。
- **登录现状**：输入字母数字用户名即创建/加载存档（`data/user_data/<用户名>.json`），**无密码、无鉴权**——这正是 S10 要堵的红线。
- **已确认决策（本 sprint 立项时与用户敲定）**：
  - 鉴权方式 = **密码账号 + 会话 token**（首次登录即注册，后端存盐哈希，读写存档需登录后签发的 token）。
  - 部署 = **只加固不实施**——把后端加固到「可安全部署」状态，部署方案只写文档（自部署 + serverless 各列要点），不绑定具体平台、不真正上线。

---

## 🎯 当前活动 Sprint：S10 — 后端加固 & 安全（上线前置）

**目标**：堵死审计两条安全红线（无鉴权、debug=True）+ 存档原子写/并发保护 + CORS/host 收敛 +
部署方案文档，达到「可安全部署为单人在线版」。**铁律照旧**：engine 纯净、依赖只向下、不破坏既有 305 测试与生产构建。

### 任务清单

- [x] **S10-T1｜存档接口鉴权（密码账号 + 会话 token）**
  - 目标：消灭「任意用户名免密读写任何人存档」。引入密码账号：首次用某用户名登录即注册（后端用 `werkzeug.security` 存盐哈希），登录成功签发会话 token；`GET/POST /api/user/data` 必须携带有效 token，token 解析出的用户名必须与被读写的存档用户名一致。
  - 涉及面：`backend/server.py`（新增 `/api/auth/login` 注册/登录 + token 签发与校验；读写接口加 token 闸）、`api/index.py`（同源加固，serverless 变体保持一致）、前端 `userStore.login` 改签名带密码、登录 UI 加密码框、`infra/persistence/api.ts` 读写带 `Authorization` 头。
  - 既有 passwordless 存档（`data/user_data/*.json` 现有 4 个）：**首次登录认领**——该用户名首次带密码登录时把密码哈希落盘归属该账号（claim-on-first-login），文档说明。
  - 验收：见「验收命令」中 `backend/test_security.py` 的鉴权断言全 PASS（未带 token 读 → 401；错密码 → 401；对密码 → 拿到 token；token 读写自己存档成功；用 A 的 token 写 B 的存档被拒）。

- [x] **S10-T2｜关闭 debug 模式**
  - 目标：`backend/server.py` 与 `api/index.py` 的 `app.run(debug=True)` → `False`（或经环境变量门控，默认 False）。生产不暴露 Werkzeug 调试器/代码执行面。
  - 验收：`grep -n "debug=True" backend/server.py api/index.py` 零命中；`backend/test_security.py` 断言 `app.debug is False`。

- [x] **S10-T3｜存档原子写 + 版本号 + 并发保护**
  - 目标：防「写入截断损坏」与「后写覆盖」。写存档改为**写临时文件 + `os.replace` 原子替换**（同目录）；payload 带 `saveVersion`（单调递增）；保存时若客户端基线版本 < 服务端当前版本 → 拒绝（409）防丢更新（前端已有 S5 保存串行合并兜单客户端，本任务堵多客户端/异常截断）。
  - 涉及面：`backend/server.py`、`api/index.py` 的 `save_user_data`；前端存档协议（`infra/persistence/schema.ts` + `migrations.ts` + `stores/persistence.ts` 装配器三处同改，加 `saveVersion` 字段，旧档迁移缺省为 1，**不破坏现有 v4 往返保真测试**）。
  - 验收：`backend/test_security.py` 断言——模拟写入中途异常后原存档仍完整可读（非截断）；旧版本号 POST 返回 409；新版本号 POST 成功。前端 `npm run test` 含 saveVersion 往返/迁移测试全绿。

- [x] **S10-T4｜CORS 收敛 + vite host/allowedHosts 收敛**
  - 目标：`api/index.py` 的 `CORS(app)`（当前全开）收敛为环境变量配置的允许源（默认本地开发源）；`vite.config.ts` 的 `host: '0.0.0.0'` / `allowedHosts: true` 收敛为默认本地、经环境变量可放开（保留隧道场景注释，不写死全开）。
  - 验收：`api/index.py` 不再裸 `CORS(app)` 无参全开；`vite.config.ts` 默认不再 `allowedHosts: true` 硬编码（改为可配置，默认收敛）；`npm run build` 通过、`npm run dev` 本地仍可起（type-check/build 不报错即可）。

- [x] **S10-T5｜部署方案文档（不实施）**
  - 目标：新建 `docs/部署方案.md`，写两条路径的要点清单（**不真正部署**）：①自部署（gunicorn/waitress + 反向代理 + 环境变量管密钥/CORS + debug off + 存档目录权限）；②Vercel serverless（复用 `api/index.py`，点明 `/tmp` 易失需接外部存储如 KV/Postgres，存档持久化要改）。文末给「单人在线版上线清单」对照本 sprint 已加固项。
  - 验收：`docs/部署方案.md` 存在且含①②两节 + 上线清单；`docs/README.md` 文档索引加一行指向它。

- [x] **S10-T6｜回归与防漂移收口**
  - 目标：全部「验收命令」绿；`docs/项目审计报告` 安全章节对应项可勾（无鉴权 / debug 两条红线已解）；FUTURE.md 的 S10 五项勾掉、进度总览 S10 标 ✅。
  - 验收：`npm run type-check` 0 错、`npm run test` 全绿、`npm run build` 通过、`python backend/test_security.py` 全 PASS；FUTURE.md S10 状态已更新。

---

## ✅ 验收命令

> Evaluator 必须**亲自重跑**以下每一条，记录实际输出。前端命令在 `frontend-vue/` 下跑；后端命令在仓库根跑（Windows PowerShell 环境，路径用反斜杠或正斜杠均可）。

```bash
# 1. 前端类型检查（期望 0 错误）
cd frontend-vue && npm run type-check

# 2. 前端测试（期望全绿，含新增的 saveVersion/auth 相关测试；不得低于既有 305）
cd frontend-vue && npm run test

# 3. 前端生产构建（期望成功）
cd frontend-vue && npm run build

# 4. 后端安全自检（自带 Flask test_client 的独立脚本，期望进程退出码 0 且打印全部 PASS）
#    覆盖：未带 token 读存档→401；错密码→401；对密码→签发 token；token 读写自己存档成功；
#    用 A 的 token 写 B 存档被拒；app.debug is False；原子写（模拟中途失败后原档不损坏）；旧版本号 POST→409
python backend/test_security.py

# 5. debug 关闭核验（期望零命中）
grep -rn "debug=True" backend/server.py api/index.py
```

**通过标准**：命令 1/2/3/4 成功（4 的脚本退出码 0、全 PASS），命令 5 零命中，且 S10 全部 `[ ]` 已勾 `[x]`。

---

## 🔁 Evolution 第 1 轮追加任务（基于 Evolution 审计，2026-06-16）

> 主题：给"玩法齐全但没动机"的产品装**留存引擎**。取 reviewer 最高 ROI 组合 🔴-1+🔴-2 + 共享埋点的 🟡-3。

- [x] **E1-T1：每日任务 + 每日登录奖励**
  - 目标：新建 `stores/daily.ts`（跨天归零，复用 shop todayKey 模式）+ 主页「今日任务」面板；3-4 任务命中现有成功点（抽卡/赢对战/收观看/养成互动）做满发奖；每日登录一次性发放。
  - 验收：面板显示进度、玩法触发自增、做满领奖、跨天重置、登录奖励跨天再发；store 三件套 + 测试；进存档 v6 跨重开保真。
- [x] **E1-T2：图鉴/收集完成度 + 里程碑**
  - 目标：`CollectionsView` 加「图鉴」tab，各维度完成度（X/总数，`.length` 派生）+ 未拥有灰位（复用 VirtualGrid）+ 静态里程碑发奖（已领 id 进存档）。
  - 验收：完成度与灰位正确、里程碑可领且持久化、完成度纯派生、进存档 v6、特征测试。
- [x] **E1-T3：成就系统**
  - 目标：`stores/achievements.ts` + 成就墙；~15-20 条静态成就在同批成功点检测解锁，发徽章+奖励，存已解锁 id。
  - 验收：解锁+发奖+持久化、成就墙可见、特征测试。

**第 1 轮验收命令**：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（≥310 全绿 + 新增）/ `npm run build`（通过）。

---

## 🔁 Evolution 第 2 轮追加任务（基于 Evolution 审计 Round 2，2026-06-16）

> 主题：让"图鉴"从只读面板进化成**全产品价值枢纽**。取 reviewer 最高 ROI 组合 🔴-1 经济出口 + 🟡-3 差异化数据。

- [x] **E2-T1：图鉴定向解锁（经济闭环）**
  - 目标：CodexPanel 灰位未拥有卡可花知识点直接解锁（addCard 入库）。userStore 加 `unlockCodexCard` 编排；定价静态表（config/codexUnlock.ts），**UR 远贵、阶梯递增、明显高于回收**，不架空抽卡。
  - 验收：解锁扣费+入库+完成度联动、余额不足/已拥有给提示不扣、走门面存档、无新 schema、三分支特征测试。
- [x] **E2-T2：真实评分/放送年可视化（差异化）**
  - 目标：CardDetailModal 加「番剧资料」区块——anime 显示真实评分/排名/放送年，character 显示作品数/人气；types/card.ts 补显式可选字段；字段缺失 v-if 守卫。
  - 验收：真实数据正确展示、缺失不显示、类型干净、颜色语义类、type-check/build 通过。

**第 2 轮验收命令**：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（≥336 全绿 + 新增）/ `npm run build`（通过）。

---

## 🔁 Evolution 第 3 轮追加任务（收口轮，基于 Evolution 审计 Round 3，2026-06-16）

> 主题：把前两轮造的系统**让新人感知到、让老玩家带出去**。取 reviewer 最高 ROI 组合 🔴-1 onboarding + 🟡-2 成绩卡（都零后端）。

- [x] **E3-T1：新人 onboarding（FTUE 点火）**
  - 目标：首登引导遮罩（localStorage 触发，指向每日任务/抽卡/收藏/对战，可跳过）+ 首抽庆祝（GachaResultModal 叠特殊态）+ 空态 CTA（7 处死文字加引导按钮）。不进存档。
  - 验收：首登弹引导可跳过且不再弹、首抽有庆祝非首抽无、主要空态有 CTA、type-check/build 通过。
- [x] **E3-T2：可分享 Wrapped 成绩卡（传播喇叭）**
  - 目标：CollectionsView 标题栏一键生成可下载炫耀图（Canvas 聚合图鉴完成度/等级/成就数/塔进度/真实数据），纯前端零后端零存档。
  - 验收：入口可见、生成含真实成绩的 PNG 可下载（toBlob+a.download）、不引 html2canvas、颜色品牌一致、type-check/build 通过。
- [x] **E3-T3：收口杂项（文档纠偏）**
  - 目标：CLAUDE.md / pitfalls.md 的存档协议版本号 v4 → v6（实际值），免后续误判。
  - 验收：文档版本号与实际一致。

**第 3 轮验收命令**：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（≥340 全绿）/ `npm run build`（通过）。

---

## 🔁 Evolution 第 4 轮（backlog 推进，tier1 off，2026-06-16）

> 推进 reviewer 前三轮提出但未做的 3 个 backlog 项（用户选定，声优收集跨栈项暂缓）。tier1 off：本节 `[ ]` 即需求源，目标驱动停。

- [x] **B1：周任务 + 连续登录递增**
  - 目标：扩 `stores/daily.ts`——加每周任务（weekKey 跨周重置，2-3 条如本周赢 5 场/抽 20 张/通塔 1 层）+ 连续登录天数 `loginStreak`（按 lastLoginDate 昨日判定递增、断签归 1）+ 递增登录奖励（第 N 天给更多）。静态定义放 config。升存档 **v6→v7**（DailySave 扩字段，schema/migrations/装配器三处同改 + 迁移 + 测试，只追加不破坏 v1~v6 断言）。
  - 验收：周任务跨周重置/做满领奖、连签递增与断签归 1、登录奖励随连签变化、进存档 v7 跨重开保真、特征测试。
- [x] **B2：番剧年表时间轴**
  - 目标：CollectionsView 加「年表」tab——把已拥有动画卡按放送年（`date` 字段，复用 evo-2 已展示的真实数据）分组成可视化时间轴（X 轴年份 1993→2025，每年拥有数/完成度）。纯派生、零后端、零存档。
  - 验收：年表按年正确分组展示拥有番剧、空态友好、纯派生（不新存字段）、颜色语义类、type-check/build 通过。
- [x] **B3：跨系统红点提示**
  - 目标：`App.vue` 侧边导航对「有奖可领/有新内容」的模块加红点角标——主页（每日任务可领/登录奖励可领）、卡牌收藏（图鉴里程碑达成未领 / 成就有新解锁未读）。daily/codex 可领态纯派生；成就「已读」态用 localStorage（设备级，仿 onboarding，不进存档），访问成就 tab 即标已读。
  - 验收：对应模块有奖可领/新解锁时亮红点、领取/查看后红点消失、成就已读用 localStorage 不升 schema、颜色语义类、type-check/build 通过。

**第 4 轮验收命令**：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（≥354 全绿 + 新增）/ `npm run build`（通过）。

---

## 🎮 Evolution 第 5–9 轮（小游戏扩展 + 持续进化，`--mode evolution --max-iter 5 --tier1 on`，2026-06-17）

> 用户指令：用 `/product-loop` 跑 **5 轮 evolution**，探索新功能。**硬性交付（贯穿全程，前 2 轮务必完成）**：
> 把小游戏做成一个**统一的「🎮 小游戏」Tab/中心**，**把现有「🎭 猜角色」迁进去**，并**至少再扩展 2 个类似猜角色的小游戏**。
> tier1 on：Evolution Reviewer 每轮探索新功能（含竞品研究），Planner 选定，Generator 实装，Evaluator 独立验收，**跑满 5 轮**。

### 🔒 硬性交付（standing constraint，Planner 每轮检查直到满足）
- [x] **统一小游戏中心**：新建小游戏 Hub（建议路由 `/minigames`，导航项「🎮 小游戏」**取代**现有「🎭 猜角色」；`/guess` 保留为重定向兼容）。Hub 是游戏选择器 + 渲染选中的小游戏；现有 `GuessCharacter.vue`（501 行）原样迁入不重写。 ← **第 5 轮完成**（MiniGamesView + /minigames + /guess redirect + 导航换「🎮 小游戏」+ 猜角色原样迁入）
- [x] **新小游戏 #1**（前 2 轮内）：从下方菜单选一个真实现，进 Hub，接经济（猜对/达标走 `profile.earn` 给知识点，仿 `submitGuess`）+ 最高分持久化（存档协议同改三处或并入 guess 域）。 ← **第 5 轮完成 = 高低牌 Higher/Lower**（角色人气/番剧口碑/番剧年代三维度，连胜里程碑发奖+每日封顶，存档 v8 minigames 域）
- [x] **新小游戏 #2**（前 2 轮内）：再选一个，进 Hub，同样接经济 + 持久化。**至此 Hub 内 ≥3 个游戏（猜角色 + 2 新）= 硬性交付达成。** ← **第 6 轮完成 = 番剧问答 Quiz**（4 类真实数据派生题，连答里程碑发奖，与高低牌共享每日封顶，存档 v9 minigames.quiz）。**硬性交付达成：Hub 内 3 游戏（猜角色 + 高低牌 + 问答）。**

### 🍱 接地可行的小游戏菜单（Reviewer 可提新创意，但这些已验证数据/技术可行，纯前端零后端）
- **高低牌 Higher/Lower**：给两张卡，猜谁评分/人气/放送更高/更早（用真实 `rating_score`/`popularity_score`/`date`）。连对计 streak，错即结算。极易上瘾、复用真实数据、零图片依赖。
- **番剧问答 Quiz**：4 选 1 选择题（「X 角色出自哪部番剧」「这部番剧哪年放送」「下列谁评分最高」），题库从真实数据派生。
- **猜番剧（剪影/像素）**：复刻猜角色的像素化机制，但猜动画封面（`/data/images/anime/{id}.jpg`）。与猜角色对称，组件可大量复用。
- **记忆翻牌 Memory Match**：番剧封面配对翻牌，计时/步数计分。视觉型，复用卡图。
- **年代排序**：拖 4 张番剧按放送年排序（用 `date`）。复用 evo-4 年表的数据思路。

### 🧭 轮次意图（Planner 可据 Reviewer 报告调整，但硬性交付优先）
- **第 5 轮**：小游戏 Hub 地基 + 猜角色迁入 + 新游戏 #1（建议高低牌）。
- **第 6 轮**：新游戏 #2（Reviewer 选定）。**硬性交付达成。**
- **第 7–9 轮**：Evolution Reviewer 自由探索更广的新功能并实装——可以是更多小游戏、小游戏与每日任务/成就的联动（如「每日小游戏挑战」）、小游戏积分榜/统计、或别的产品进化方向。每轮取最高 ROI。

### 通用约束（全 5 轮）
- 架构铁律不破：engine 纯净、依赖只向下、颜色语义类（禁 text-white 压浅底/禁动态色类）。游戏逻辑放 store/纯函数，可注入 RNG 更佳（便于测试）；现有 guess 在 store 内 `Math.random` 是既有先例，新游戏纯逻辑尽量抽纯函数 + 注入 RNG 并配特征测试。
- 经济安全：奖励只走 `profile.earn`；别造可刷分无上限的经济漏洞（设每日上限或递减，仿猜角色 score÷2）。
- 存档：新游戏最高分/统计若要持久化，按既有「三处同改 + 迁移 + 测试」升 schema（现 v7）。设备级 UI 偏好用 localStorage。
- 验收命令（每轮）：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（全绿 + 新增）/ `npm run build`（通过）。截图工具在本环境失效，UI 验证靠 type-check/build + 运行期 DOM eval（live 起服务后 `preview_eval` 读 innerText）。

---

## 🔁 Product-Loop 第 1 轮追加任务（mode=all 三审综合，2026-06-24）

> 主题：三视角高度收敛——**真实 Bangumi 数据 + 已写好的推荐引擎被埋没；产品价值从「收集库存」升级为「番剧品味身份」**。
> 本轮取最高 ROI 组合：先修一个真 CSS bug 止血，再把闲置的推荐引擎接进主循环 + 让「看过」成为第一公民喂养它。
> 全部纯前端、零后端、零存档改动、不破架构/经济铁律。这是 tier1-on 5 轮迭代的第 1 轮，其余方向留后续 4 轮。

- [x] **I1-T1：修复无效 CSS 变量（小游戏中心质量止血）**
  - 目标：让小游戏相关界面的边框/次级正文色/圆角重新吃到皮肤令牌系统，消除「不报错但默默坏色/丢圆角」的静默缺陷。
  - 验收：小游戏相关界面在 5 套皮肤下边框与次级正文色随皮肤变化（不再落到 currentColor/透明）、圆角随皮肤变化（不再恒为硬编码 fallback）；全仓不再存在「引用未定义令牌名」的用法；type-check / build 通过。

- [x] **I1-T2：「看过」升格为第一公民（推荐引擎的输入地基）**
  - 目标：把目前只服务一个小游戏的「看过」数据升格为可被主循环多处消费的公共状态，让玩家在图鉴/收集里直观看到「我看过哪些」，并能喂养发现/推荐。
  - 验收：图鉴/收集中已「看过」的番有可见区分标识；新增至少一处「看过」维度的可见进度/里程碑（纯派生，零存档改动，无需升 schema）；既有「看过」读写与持久化不回归；type-check / test / build 通过。

- [x] **I1-T3：发现/推荐引擎落地到主循环**
  - 目标：把已写好却只接在一个小游戏里的内容推荐能力接进高频路径，给抽卡与收集一个「方向感」——「因为你看过/拥有 X，这些你还没有的可能合你口味」。
  - 验收：至少两个高频出口（如收藏/图鉴侧、抽卡结果后）出现「为你推荐」并给出推荐理由（命中口味标签）；新号或无「看过」时有合理空态引导（不报错、不空白死胡同）；推荐为纯派生且含必要记忆化；type-check / test / build 通过。

- [x] **I1-T4：内容详情补字段 + 关联索引（详情页收窄版）**
  - 目标：补齐详情里数据已有、类型已声明却未呈现的角色资料字段；建立供发现引擎/下游复用的内容关联索引，避免下游每次渲染全量重算。（注：内容详情 audit 已 STALE——详情已展示简介/评分/排名/年代/登场作品/标签，本轮不新建详情页；声优维度因后端剥离需后端配合，留 backlog。）
  - 验收：补充展示的角色资料字段有数据时正确显示、无数据时不显示（守卫）；关联索引可被发现引擎/详情复用且不引发渲染卡顿；type-check / build 通过。

---

## 🔁 Product-Loop 第 2 轮追加任务（mode=all 三审综合，2026-06-24）

> 主题：**收尾第 1 轮的投资**——发现引擎已接上，但「看过」只升了读没升写、且存在两套割裂的看过源，
> 导致引擎长期空输入 / 被随机库存稀释。本轮把燃料管道打通（修排除 bug + 拓宽看过写入口 + 统一两套看过为派生合集），
> 并顺手做一个基建已齐、ROI 最高的传播子项（品味人格/题材显形到分享卡与主页 + 一键分享）。
> 全部纯前端、零后端、**零存档改动**（SAVE_VERSION 仍 12，schema/migrations/装配器三处不动）、不破架构/经济/颜色铁律。

- [x] **I2-T1：修复推荐引擎「排除已拥有/已看」契约 bug（必修）**
  - 目标：让发现/推荐只推荐玩家还没拥有、还没看过的番，不再把已在库/已看的番推回来——尤其修复图鉴出口（种子=看过∪已拥有全集，候选池却把其它已拥有/已看番照样推回）这一破契约的真 bug。
  - 验收：至少两个推荐出口（抽卡结果 / 图鉴动画域）的推荐结果中不再出现玩家已拥有或已看过的番；新增/扩展测试覆盖「排除非种子的已拥有/已看番」；不破坏既有推荐测试（独立的口味推荐路径无此 bug，不应被改签名连累）；零存档；type-check / test / build 通过。

- [x] **I2-T2：拓宽「看过」写入口（一处覆盖全站）**
  - 目标：让玩家在最高复用面的内容入口处就能就地把一部番标记为「看过 / 取消看过」，不必绕进品味画像小游戏深层页——把「看过」从一个小游戏的内部勾选，变成全站可达的第一公民动作。仅对番剧有效（角色无「看过」语义）。
  - 验收：在一个被推荐条/图鉴/品味页/对战多处复用的内容详情入口，番剧可见「看过」开关并能切换；切换即时反映到看过状态（印章/计数/推荐输入随之变化）；角色不出现该开关（维度守卫）；未登录态延续「仅本次会话有效」的既有提示语义（不误导已持久化）；既有看过读写与持久化不回归；零存档；type-check / test / build 通过。

- [x] **I2-T3：统一两套「看过」为派生合集（零存档，只动读侧）**
  - 目标：消除「队列看完的番」与「品味画像勾选的番」两套割裂看过源，让人格/推荐种子/看过印章/段位/首页看过计数等所有读取侧统一消费两者的并集——引擎吃满玩家全部观看历史。写侧维持现状各写各的语义事件，不合并写、不升档。
  - 验收：上述各读取侧的「看过」口径变为两套来源的并集；写路径与存档字段完全不动（SAVE_VERSION 不变，schema/migrations/装配器三处零改动）；段位/引导文案不再把「看过」唯一指向品味画像页（放宽为含队列看完）；合集派生不引入两个 store 互相 import（依赖只向下）；既有持久化往返保真测试不回归；type-check / test / build 通过。

- [x] **I2-T4：品味人格/题材偏好显形到分享卡与主页 + 一键分享**
  - 目标：把已算好却埋在三级小游戏页的「番剧品味身份」（人格标签 + 题材偏好 + 小众度）显形到高曝光、可传播的位置——注入 Wrapped 分享成绩卡与主页，并让出图支持系统级一键分享（支持的设备直接调起分享面板，不支持则回落现有下载）。让「我是什么口味的动画宅」成为可炫耀、可传播的身份资产。（注：人格升格拆 3 子任务，本轮只做①Web Share 一键分享含回落 +②人格/题材注入 Wrapped+主页 chip；③多轴人格雷达留第 3 轮。）
  - 验收：分享成绩卡上出现品味人格/代表题材标签（数据来自既有品味报告纯函数，缺数据不显示）；主页可见一处品味身份呈现（chip/卡片）；出图分享在支持的环境调起系统分享面板、不支持时回落到既有下载（用户取消不报错）；不引入 html2canvas、首版不嵌远程封面图（避免 canvas 跨域 taint）；分享图聚合逻辑维持纯函数 + 测试；颜色走语义令牌；零存档；type-check / test / build 通过。

**第 2 轮验收命令**：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（≥448 全绿 + 新增，不弱化既有）/ `npm run build`（通过）；回归基线 `python backend/test_security.py`（全 PASS、退出码 0）/ `grep -rn "debug=True" backend/server.py api/index.py`（零命中）。**零存档铁律**：SAVE_VERSION 仍 12、schema/migrations/装配器三处无改动。

---

## 📦 Backlog（决策门控，**本轮未激活**，勿当作 `[ ]` 执行任务）

> 以下是 FUTURE.md S11/S12 的拆分，**仅作路线参考**。它们是「演进/终点」方向，FUTURE.md 明确标注
> 「到达时再 `/think` 细化，依赖届时的决策」。**未列为 `[ ]` 是有意为之**——一个 product-loop 周期
> 无法完成整库 React 迁移或多人后端；激活前需单独 `/think` 立项并把当时确定的子任务搬到上面的活动区。

### S11 — React 视图迁移（演进，前置依赖：S2–S5 的 engine 已干净 ✅）

> 拆分粒度已细化（满足 `/goal` 的「每一任务拆分到 SPRINT」）；**仍为决策门控、未激活**——激活前需 `/think` 敲定下列「决策门」，再把选定方案的任务搬到上面活动区。

- [ ] **S11-T1｜React 骨架 + 共享层零改动复用验证**
  - 目标：Vite + React + TS 新应用（建议 `frontend-react/` 与 Vue 版并存），`@/` 别名指到现有 `engine / types / config / data / infra`，证明这四层**零改动**能在 React 构建下编译运行。
  - 验收：React 应用启动；`import` engine 纯函数（如 `battleCalculator`/`gacha/draw`）+ types + config 无需改动即编译；一个 smoke 路由渲染主数据。
- [ ] **S11-T2｜状态层移植（Pinia → Zustand/Jotai，薄编排）**
  - 目标：领域 store 改用选定状态库重写为「薄编排」，规则仍只在 engine，不回写视图层。先移植 `profile + collection`（含 `spend/earn` 唯一货币入口）打样。
  - 验收：打样领域行为与 Vue 版一致（货币/收藏增删的特征测试通过）；store 不含规则逻辑。
- [ ] **S11-T3｜持久化层复用 + React 适配**
  - 目标：`infra/persistence`（schema/migrations/api）原样复用；写 React 侧装配器（对应现 `stores/persistence.ts`，含保存串行合并 + 乐观并发 saveVersion）。
  - 验收：存档 v9 往返保真（buildPayload⇄applyPayload 对照测试）；与现后端鉴权（S10 token）打通。
- [ ] **S11-T4｜8 模块逐页 React 重写**
  - 目标：Home/Gacha/Collections/Battle/SquadBattle/Nurture/MiniGames/Settings 八页按域逐个 React 化（皮肤令牌系统 `assets/skins.css` 复用，data-skin 切换）。
  - 验收：**逐页**与 Vue 版功能对齐（每页一条走查清单）；颜色铁律（语义类/禁 text-white 压浅底）延续。
- [ ] **S11-T5｜App 壳 + 路由 + 启动门控 + onboarding**
  - 目标：React Router（或 TanStack Router）+ App 数据门控（仿 S9 非阻塞挂载 + 30s 超时 + 失败重试）+ onboarding/红点等设备级 localStorage 状态迁移。
  - 验收：启动 API 恰 2 次（无双拉）；路由懒加载；首登 onboarding 与红点行为一致。
- [ ] **S11-T6｜对照验收 + engine 复用率统计**
  - 目标：React 版与 Vue 版功能逐页验收对齐；统计 engine/types/config/data/infra 的复用率。
  - 验收：**功能对齐；engine 复用率 > 50%**（FUTURE.md 的 Exit）；生产构建通过；测试不低于现 395 的等价覆盖。
- **决策门（`/think` 先定）**：① 状态库 Zustand vs Jotai；② 路由 React Router vs TanStack Router；③ 双栈并存渐进迁移 vs 大爆炸切换；④ 组件方案（纯 Tailwind vs headless 库）；⑤ 拖拽密集 UI（卡组/战斗）的 React 实现（dnd-kit 等）。

### S12 — 权威后端 & 多人/PvP/排行榜（终点）

> 拆分粒度已细化；**决策门控、未激活**。前置依赖 S10（鉴权/原子写已就绪）+ engine 的 RNG 可注入（S1 起为服务端权威铺路）。

- [ ] **S12-T1｜engine monorepo 化（前后端共享包）**
  - 目标：`engine`（+ types/config/data 必要部分）抽成独立包，pnpm/turbo workspace，前端与 Node 服务端共同消费同一份规则。
  - 验收：engine 作为包被 Node 服务 import 并跑通一个纯函数（如 `resolveClash`）；前端改为依赖该包后测试仍全绿。
- [ ] **S12-T2｜Node 权威服务端骨架（服务端结算 + 客户端预测）**
  - 目标：Node 服务端权威结算战斗/抽卡（用服务端种子 RNG），客户端预测 + 对账；复用 S10 的鉴权/会话概念。
  - 验收：一场宅理论战由服务端权威结算，客户端预测后与服务端结果对账一致；抽卡保底状态服务端权威。
- [ ] **S12-T3｜存档迁移到数据库**
  - 目标：用户/存档从 `data/user_data/*.json` 迁到数据库；保留 schema 版本化迁移路径（现 v9）。
  - 验收：存档读写走 DB；现有文件存档可迁入；鉴权 token 仍生效；并发保护（乐观 saveVersion）在 DB 层成立。
- [ ] **S12-T4｜排行榜（战绩/收集进度）**
  - 目标：服务端聚合战绩与收集完成度，排行榜 API + 前端榜单页。
  - 验收：榜单按战绩/收集正确排名；分页/刷新；数据来自服务端权威记录（防客户端伪造）。
- [ ] **S12-T5｜PvP 匹配 + 对战**
  - 目标：匹配队列 + 实时对战同步（服务端权威结算，反作弊靠服务端 RNG），交互技 PvP 时改对端决策（S8c 已预留 ctx.rng 注入点）。
  - 验收：两客户端匹配成功并完成一场同步对战；断线/超时兜底；结果服务端权威。
- **决策门（`/think` 先定）**：① 数据库选型（Postgres/SQLite/KV）；② 实时传输（WebSocket/SSE/轮询）；③ 匹配机制（简单队列 vs ELO）；④ 预测-回滚策略；⑤ 部署架构（与 S10 部署方案衔接）。

## 🔁 Product-Loop 第 3 轮追加任务（mode=all 三审综合，2026-06-24）

> 主题：把上一轮第一次闭合的「发现→收下→表达」核心循环，从"能用"做到**"上瘾/想晒"**，并**弥合多轮未动的 rarity 价值观裂缝**。
> 三视角收敛：必修 C1（看过口径 bug 止血）+ 多轴人格雷达做透（病毒传播，库已就绪）+ 小众佳作雷达（research 最高价值，纯派生弥合裂缝）+ 发现卡就地落子（收下段临门一脚）+ 保底进度可视（含顺手两桶硬色还债）。
> 全部纯前端、零后端、**零存档改动**（SAVE_VERSION 仍 12，schema/migrations/装配器三处不动）、不破架构/经济/颜色铁律。题材集册留第 4 轮（长线牵引轮），硬色大头/onboarding/家园冻结/文档同步留第 5 轮（打磨/还债轮）。

- [x] **I3-T1：统一「看过」口径（必修 bug）**
  - 目标：消除"同一部番在图鉴显『看过』印章、点开详情却显『未看』"的口径裂缝。详情看过开关的**显示态**改读全站统一的看过并集口径（与图鉴印章/段位/主页计数对齐）；**写入态维持现状**（仍写手动集，不合并写、不改写侧语义）。
  - 验收：通过观看队列看完的番在详情开关显示为"已看过"态（与图鉴印章一致）；点击切换仍只写手动集（写侧语义不变）；角色不出现该开关（维度守卫）；既有看过读写与持久化不回归；零存档（SAVE_VERSION 不变、三处装配器零改动）；type-check / test / build 通过。

- [x] **I3-T2：多轴动画人格雷达**
  - 目标：把已算好的品味报告里 4–5 个可成轴信号（小众度、阅片广度/覆盖、高分偏好、题材专精、来源/年代多样性）各折成 0–100 的人格轴，用雷达呈现，注入**品味画像报告页的屏幕渲染区**，把"可传播"的身份资产升级为"立体、值得传播"的画像。**雷达不进分享卡/出图 Canvas。**
  - 验收：报告页出现多轴雷达、各轴值来自既有品味报告纯函数派生且落在 0–100；缺数据/新号样本不足时不报错（合理空态/引导态）；雷达颜色随皮肤令牌变化（5 套皮肤）、不糊数据标签；轴归一逻辑是纯函数 + 特征测试；不进分享卡出图；零存档；type-check / test / build 通过。

- [x] **I3-T3：小众佳作雷达（高分低人气未拥有番）**
  - 目标：新增一处"发现"栏，纯派生按"评分高 ∧ 人气低 ∧ 玩家未拥有"排序，把经济上被 rarity 贬低的冷门佳作在发现层显形、表彰，让"抽卡权重价值观"与"图鉴价值锚价值观"第一次在前端对齐（rarity 裂缝唯一零后端的弥合口）。
  - 验收：发现栏稳定展示一批"高分低人气且玩家未拥有"的番、不混入已拥有/已看番、不混入评价人数过低的无效样本；排序逻辑纯派生且有记忆化（不每帧全库重排）；空态/无合格项不报错；颜色走语义令牌；零存档；type-check / test / build 通过。

- [x] **I3-T4：发现卡就地落子（推荐条加一步动作）**
  - 目标：把推荐条上"整卡只能点进详情"的单一动作，扩成"主体区点击仍进详情 + 卡上就地一步『看过』『解锁』"，把"收下"从绕进详情的多步操作变为发现处一步完成；发现引擎从"只读"变"可按"。复用既有看过/解锁门面（经济安全与余额校验已内建）。
  - 验收：推荐条每张卡可就地"看过"切换与"解锁"（成功入库 / 余额不足或已拥有给提示不扣费）、主体区点击仍进详情；未登录/已拥有/余额态有正确守卫与禁用/提示；各复用出口（抽卡结果/收藏/图鉴）均不回归（推荐"不推已拥有/已看"契约不破）；无嵌套可点元素 a11y 隐患；就地"看过"显示态与 I3-T1 统一口径一致；零存档；type-check / test / build 通过。

- [x] **I3-T5：抽卡保底进度可视 + 顺手两小桶硬色还债**
  - 目标：①把抽卡 UP 横幅从"只用文字讲保底规则"升级为"显示距硬保底还差几抽"的 live 进度（仅一条 **70 硬保底**进度条，区分番剧/角色域各自的保底状态——**无软保底，"软保底 10" 是误记，勿造**）；②**顺手**把本任务必然触碰的 UP 横幅、以及上轮刚改过仍留硬色的观看统计两处做最小硬编码色还债（迁皮肤令牌；稀有度/资源识别色按铁律保留为固定例外）。
  - 验收：UP 横幅显示 live 保底进度（距保底剩余抽数随抽卡实时变化、番剧/角色域各读各的保底状态）；UP 横幅与观看统计两处的可迁移硬色改为语义令牌或皮肤变量（稀有度识别色等固定例外保留）；进度只读不写（零存档）；颜色铁律不破（禁 text-white 压浅底/禁动态拼色/禁未定义令牌）；type-check / test / build 通过。

**第 3 轮验收命令**：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（≥456 全绿 + 新增，不弱化既有）/ `npm run build`（通过）；回归基线 `python backend/test_security.py`（全 PASS、退出码 0）/ `grep -rn "debug=True" backend/server.py api/index.py`（零命中）。**零存档铁律**：SAVE_VERSION 仍 12、schema/migrations/装配器三处无改动。

---

## 🔁 Product-Loop 第 4 轮追加任务（mode=all 三审综合，2026-06-24）

> 主题（**长线牵引轮**）：连续四轮闭环已无结构断裂，唯一裸露的大空缺 = **长线/终局真空**——每日任务近峰与「集满 968」远峰之间没有中目标牵引。本轮补这段缺失的「中程目标层」：
> 头号主菜 = **题材集册 Genre Sets**（complete-the-set 留存机制，纯派生零存档，蓝图就绪）；配菜 = 设备级**「正在追」Pin**（自设目标）+ **详情相似作品/集册缺口一步收下**（承接第 3 轮延期项 + 让集册可「就地补番」）。
> **核心护栏（成败分水岭）**：题材集册做**展示墙，不是待办地狱**——按玩家专精题材排序、只突出在意的、不强制集满、无 FOMO 倒计时、**纯展示不发奖不持久化**（发奖/记已领=触 schema，本轮禁止）。否则触发 completionist burnout，功能从「上瘾」退化为「劝退」。
> 全部纯前端、零后端、**零存档改动**（SAVE_VERSION 仍 12，schema/migrations/装配器三处不动；Pin 走设备级 localStorage）、不破架构/经济/颜色铁律。硬色全量（养成 102 处 + CardDetailModal 残留）/ confirm-alert 替换 / onboarding 点名品味 / 文档版本同步 v12 / 家园冻结，全部留第 5 轮（打磨/还债轮）。

- [x] **I4-T1：题材集册 Genre Sets（长线中目标墙，纯展示零存档）**
  - 目标：在图鉴动画域新增一处「题材集册」——把可玩池的每个干净题材标签做成一个集册，显示该题材的拥有/总数完成度条 + 纯派生段位称号（仿「看片里程碑」的纯展示阈值，如青铜/白银/黄金），并标注「还差 N 部」。集册按**玩家最强题材**（品味报告的专精题材顺序）排到前面，把系统目标变成「我在意的自设目标」；其余集册按桶规模/接近完成度排，高亮最接近完成的桶以勾引「再补几部就满」。这是每日任务与集满 968 之间缺失的**中程目标层**。
  - **护栏（硬约束，写进验收）**：①集册是**展示墙非待办地狱**——按 persona 专精轴排序而非字母/数量机械排（research 明确这是成败分水岭）；②**不强制集满、无 FOMO 倒计时**；③**纯展示不发奖、不记已领、不持久化**——完成度与段位全纯派生，**绝不复用图鉴里程碑的「领取制」**（领取/记已领=进存档=升 schema，本轮禁止）。若产品坚持发奖/记已领，必须明确上报升 v13（三处同改），不擅自做。
  - **数据现实**：可玩池剔噪后题材标签是个干净小集（数量级温和、不会刷屏），按桶规模降序取一批高质量集册即可，**不需要复杂的稀疏长尾过滤**；桶维度必须用**剔噪后的题材标签集**，不能用未剔噪的裸标签（否则会冒出「TV 集册」「日本集册」这类废桶）。
  - 验收：图鉴动画域出现「题材集册」区，每个集册显示正确的拥有/总数完成度 + 纯派生段位称号 + 「还差 N 部」；集册排序把玩家专精题材顶到前面（persona 排序，非字母/机械排）；桶用剔噪题材集、无废桶（无 TV/日本/来源类噪声集册）；完成度与段位**纯派生**（不新增存档字段、不发货币、不记已领）；空/未持有集册不报错、不制造 FOMO（无倒计时、不强制集满）；集册数量级温和不刷屏；段位阈值逻辑是纯函数 + 特征测试；颜色走语义令牌；**零存档（SAVE_VERSION 不变、schema/migrations/装配器三处零改动）**；type-check / test / build 通过。

- [x] **I4-T2：详情页相似作品（详情页内导航，不叠 modal）+ 集册缺口一步收下**
  - 目标：①在番剧详情里新增「看过这部的人也喜欢」相似作品入口（复用既有内容关联引擎，单种子推荐，排除已拥有/已看）；点相似作品时**在详情页内导航/替换当前内容**，而不是再叠一层弹窗。②让 I4-T1 的集册缺口（「还差 N 部」里的具体番）可**就地一步收下**（看过 / 解锁，复用既有看过/解锁门面，经济安全已内建），把「补集册」从绕进多层页变成发现处一步完成。承接第 3 轮延期的「详情相似作品」并与集册主线联动。
  - **关键约束（写进验收）**：相似作品**不能直接复用现成推荐条组件嵌进详情**——该组件自带详情弹窗，嵌进去会叠到第三层 modal；必须走**详情页内导航/替换内容**（点一部相似作品→详情内容切换为那部，不再开新层）。这同时顺手缓解既有「嵌套 modal」债。相似作品仅番剧维度显示（角色无题材推荐语义）。
  - 验收：番剧详情出现相似作品入口、推荐结果排除已拥有/已看番；点相似作品在详情页内切换内容（不新增叠层弹窗，不出现三层 modal）；切换后详情各派生信息（拥有数/看过态/技能等）随之正确刷新；角色详情不出现相似作品（维度守卫）；集册缺口的番可就地「看过 / 解锁」（成功入库 / 余额不足或已拥有给提示不扣费，未登录守卫）；各推荐/集册出口「不推已拥有/已看」契约不破；无嵌套可点元素 a11y 隐患；就地「看过」显示态与全站统一口径一致；**零存档**；type-check / test / build 通过。

- [x] **I4-T3：设备级「正在追」Pin（自设目标，localStorage）**
  - 目标：让玩家在番剧详情就地把一部番标为「正在追」，并在主页有一处轻量「正在追」列表 + 「看完」按钮——把「我现在想推进的目标」显形为设备级自设目标，补全「系统目标墙（集册）+ 玩家自设目标（Pin）」的双轨。设备级先例（onboarding/成就已读/主题）已验证此类「记住一次」状态走 localStorage、**不进存档协议**。
  - **护栏（写进验收）**：①**轻量铁律**——一个标记 + 主页一个列表 + 一个「看完」按钮，不做成新系统；②**少量上限**防「死亡清单」（MAL/AniList 式永远清不完的列表）；③对极贵目标（如 UR 解锁价高昂）给**诚实但不绝望**的双路径提示（不退化成纯挫败）；④跨设备不同步是设备级固有取舍，可接受。
  - 验收：番剧详情可「正在追」开关并切换、有少量上限；主页出现「正在追」列表 + 「看完」按钮（看完→写看过并取消 Pin）；进度纯派生（未拥有贵卡显诚实的获取路径提示，不纯挫败）；角色不出现该开关（维度守卫）；Pin 走设备级 localStorage（仿既有设备级先例，try/catch，无 DOM 静默回落）、**不进存档协议、不升 schema**；既有看过/持有读写不回归；**零存档（SAVE_VERSION 不变、三处装配器零改动）**；type-check / test / build 通过。

- [x] **I4-T4：ViewingStats 题材显示源收口（换源不删字段）+ CardDetailModal 残留硬色顺手清**
  - 目标：①把观看统计里的题材进度显示源，从存档里那个旧统计字段，换成更准、已剔噪的品味报告题材源（消费端换显示数据源，体验更一致）。②做 I4-T2 必然触碰详情组件时，**顺手**把详情组件里残留的几处硬编码色迁到语义令牌（稀有度/资源识别色按铁律保留为固定例外）。
  - **关键约束（写进验收，来自 scout 纠偏）**：那个题材进度字段是**进存档的持久字段**（走 schema/migrations/装配器/往返测试四处），**不是孤立 computed**——本轮**只改消费端显示数据源，保留字段本身**。**切忌顺手删该字段**（删字段=动 schema=升档，本轮禁止）。彻底删字段留到债轮升档。
  - 验收：观看统计题材进度显示改用剔噪品味报告源、显示更一致；**存档字段本身保留**（不删、不改 schema/migrations/装配器）；详情组件残留可迁硬色改为语义令牌（稀有度识别色等固定例外保留）；颜色铁律不破（禁 text-white 压浅底/禁动态拼色/禁未定义令牌）；**零存档（SAVE_VERSION 不变、三处装配器零改动）**；type-check / test / build 通过。

**第 4 轮验收命令**：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（≥466 全绿 + 新增，不弱化既有）/ `npm run build`（通过）；回归基线 `python backend/test_security.py`（全 PASS、退出码 0）/ `grep -rn "debug=True" backend/server.py api/index.py`（零命中）。**零存档铁律**：SAVE_VERSION 仍 12、schema/migrations/装配器三处无改动。

---

## 🔁 Product-Loop 第 5 轮追加任务（mode=all 三审综合 · 债轮 · 2026-06-24）

> 主题（**最后一轮 = 打磨/还债轮**）：把地板抬平、清技术债，**让前 4 轮的投资被新用户感知**。三视角收敛：硬色连根拔（背刺 5 套换肤卖点）+ confirm/alert 统一弹窗（含共享解锁门面，一笔还两债）+ onboarding 点名品味身份（5 轮差异化对新用户隐形 = 最高 ROI）+ 收尾杂项三连（文档 v12 / 家园冻结 / 死代码修）。
> 债轮项多但低风险，故纳 5 项任务、把快赢打包。全部纯前端、零后端、**零存档改动**（SAVE_VERSION 仍 12，schema/migrations/装配器/codex 四处不动；统一弹窗组件/共享色映射/onboarding 步/死代码修全纯前端）、不破架构/经济/颜色铁律。
> **排期硬约束**：先抽共享基建（共享色映射 + 统一弹窗 composable + 共享解锁门面），再让各处消费——避免逐处补导致漏/复发。
> **本轮明确不做**：品味社交亲和度对照（玩家间品味重合度/社交化发现）= 5 轮后最大剩余机会，需后端 + 跨栈（触 schema）→ 归 S12 backlog 路标，债轮不碰。

- [x] **I5-T1：养成+挑战塔硬色 138 处连根拔（抽共享色映射 + 整函数重写）**
  - 目标：把养成簇（107 处 / 5 文件：CharacterProfile/NurtureActions/InteractionPanel/CharacterSelector/DialogueSystem）+ SquadBattleView（31 处）的硬编码颜色迁到皮肤语义令牌，使养成页与挑战塔随 `data-skin` 正确换肤。
  - **WHY（写进验收的核心理由）**：这 138 处硬色不随皮肤切换 → 切 midnight（琥珀暗底）/neon（青霓虹暗底）时养成/挑战塔页炸饱和色斑，**5 套换肤卖点在这两簇直接失效**——背刺 S7 最大投入的差异化卖点。
  - **硬约束（防 partial-migration 漏/复发）**：① **必须"抽共享色映射 + 整函数重写"，不是逐处 grep-replace 硬色行**——`bondLevel` 在 CharacterProfile 与 CharacterSelector **各写一份**几乎逐字相同的硬色表、`moodStatus` 在 CharacterProfile 与 InteractionPanel **各写一份**，必须新建一份共享语义色映射（纯常量/纯函数零 Vue/IO），两处 bondLevel + 两处 moodStatus + 3 个 named 映射（INTERACTION/ACTIVITY）都改成消费它；② **partial-migration 陷阱**：这些函数已有零星几档迁了语义类、其余仍硬色——整函数重写成调共享映射，禁止"同一函数里半语义半硬色"；③ 颜色铁律不破（禁 text-white 压浅底、禁动态拼色、禁未定义令牌）。
  - **取舍声明（写进验收）**：语义档去重后羁绊 6 档会塌成 4-5 个语义色、档与档颜色会重复——这是从"6 个饱和硬色"到"5 套皮肤协调色"的必然取舍；验收看的是 **"切暗皮（midnight/neon）不炸色斑"**，不是"6 档全异色"；icon(emoji) 不变保留区分度。合法例外（保留硬色）：稀有度渐变/徽章、稀有度 shadow、`bg-gray-500` 兜底、图片压片白字。
  - 验收：养成 5 文件 + SquadBattleView 可迁硬色全部改为消费**共享语义色映射 / 皮肤令牌**（无逐处残留硬色、无半语义半硬色函数）；切 midnight/neon 皮肤养成页与挑战塔不炸饱和色斑、随皮肤协调；稀有度/资源识别色 + 图片压片白字固定例外保留；颜色铁律不破；**零存档（SAVE_VERSION 不变、schema/migrations/装配器零改动）**；type-check / test / build 通过。

- [x] **I5-T2：原生 confirm/alert 18 处统一主题化弹窗（含共享解锁门面，一笔还两债）**
  - 目标：把全仓 18 处原生 `confirm/alert` 收口为统一主题化弹窗基建；其中 4 处逐字重复的"解锁四件套"（GenreSets/RecommendationStrip/NicheGems/CodexPanel）先抽成共享解锁门面再走统一弹窗。
  - **WHY**：原生 confirm/alert 是浏览器灰框、与 5 套皮肤割裂、移动端体验差；4 处解锁逻辑逐字重复是重复概念债。一笔投入同时还**散落原生弹窗债 + 重复解锁概念债**。
  - **硬约束（写进验收）**：① **统一弹窗须新建**——全仓 13 modal 全 feature-specific，无通用 confirm/alert/toast 基建；新建挂 App 顶层的主题化弹窗组件 + composable（confirm()/alert() 返回 Promise），**走语义令牌**（禁 text-white 压浅底）；② **先抽共享解锁门面再接入**——4 处解锁确认逐字重复同一套（登录守卫→余额守卫→confirm 花费→unlockCodexCard→失败提示），抽一个共享门面收敛 4 处到一行调用，**门面必须收 domain 参数**（3 处 anime-only、CodexPanel 是 anime|character 双域，**别写死 anime**）；③ **守卫不重复漂移**——`unlockCodexCard` 已自带登录/卡片/余额守卫并返回 {ok,error}，门面以 store 返回 error 为准、UI 只负责 confirm + 展示 error；④ **store 层 4 处降级 toast**——userStore/collection/persistence/battleSetup 脱离组件上下文，不走 Promise 弹窗，降级为非阻塞 toast（复用既有 addLog 通知通道）。
  - 验收：组件层原生 confirm/alert 全部走统一主题化弹窗（语义令牌、随皮肤、移动端可用）；4 处解锁确认收敛到共享门面一行调用（门面收 domain、双域正确、以 store error 为准、无两层重复守卫）；store 层 4 处降级为非阻塞 toast（不阻断、不再弹原生 alert）；既有解锁经济安全（未登录/余额不足/已拥有守卫与不扣费）不回归；颜色铁律不破；**零存档**；type-check / test / build 通过。

- [x] **I5-T3：onboarding 点名品味身份（新增第 5 步 + value-first 改写开场）**
  - 目标：① 在 onboarding 末尾**新增第 5 步**点名"番剧人格/品味"主线（route `/collections`，cta 引导去标记看过）；② 把**开场第 1 步**从"每天来打个卡"的功能罗列改成 **value-first** 文案（"这里会照出你的番剧人格"），让前 4 轮做的差异化主线对新用户第一眼可见。
  - **WHY（最高 ROI）**：前 4 轮把"发现番剧人格/品味画像/题材集册"做成产品主线与差异化卖点，但 onboarding 4 步只讲打卡/抽卡/收藏/对战，**对新用户完全隐形** = 5 轮差异化投资白费。evolution 终评判定这是最高 ROI 项。
  - **裁决（写进验收）**：加第 5 步 **且** value-first 改写开场，二者都做但克制——末尾**只加 1 步**（不堆步数，research"每多一步都是流失点"），其余 value 靠改第 1 步文案吸收。**纳入"引导里让用户标 2-3 部看过"**：第 5 步 cta 跳 `/collections` 引导标记，避免新号落到雷达/集册**全 0 灰墙**；**但引导内不调用 toggleTasteWatched**（保持引导只导航不写状态，门面已存在用户可自行触达）。
  - 验收：onboarding 新增第 5 步点名品味/番剧人格、cta 跳 `/collections` 引导标记看过（引导内不写状态）；第 1 步开场改为 value-first 文案（不再以功能罗列开场）；步数从 4 增至 5 后进度点/「开始游玩」按钮/isLast 自动适配无逻辑错乱；完成标志走设备级 localStorage（**不进存档、不升 schema**）；新步骤颜色走语义令牌；**零存档**；type-check / test / build 通过。

- [x] **I5-T4：/homestead 完整冻结（路由守卫 redirect + 移出构建）**
  - 目标：把家园页从"导航已藏但路由仍 active"改为**完整冻结**——手敲 `/homestead` 不再渲染半成品。
  - **WHY**：现状导航已注释隐藏但路由仍注册 + lazy-import，手敲 URL 仍渲染 HomesteadView 半成品 → 新用户/分享链接可触达未完成功能、露馅。research R5.3 要"完整冻结"非只藏导航。
  - **硬约束（写进验收）**：路由层改 `redirect:'/'` + **删 component import**（HomesteadView 移出构建产物、连带省体积）；App.vue 导航注释块一并删除；HomesteadView 仅被 router 引用（无别处 import）无连带影响；**view 文件本身保留（冻结不删除，延续既往口径）**。
  - 验收：手敲 `/homestead` redirect 到首页、不再渲染半成品；HomesteadView 移出构建产物（router 不再 lazy-import 它）；App.vue 残留导航注释块已删；其余路由/导航不回归；**零存档**；type-check / test / build 通过。

- [x] **I5-T5：收尾杂项三连（文档同步 v12 + 死代码修 + setTimeout 登记）**
  - 目标：打包三类低风险还债——① 文档版本同步 v12；② 两处死代码/语义错位修；③ NurtureActions 未登记 setTimeout 收口。
  - **WHY**：文档漂移误导后续轮按旧版本判断存档结构（pitfalls 全角色必读、自身漂 6 版最危险）；矛盾文案/语义错位露给用户降低专业感；裸 setTimeout 违反计时器铁律（"有 onUnmounted"≠"所有计时器都登记了"的假安全）。
  - **子项（写进验收）**：① **文档 v12**（纯文档零代码）：`frontend-vue/CLAUDE.md` 版本沿革表 v11 → 补 v12 一行（tasteProfile 持久化）；`docs/plans/pitfalls.md` L11 + L34「存档协议 v6」→ v12（权威值 schema.ts:30 SAVE_VERSION=12）；② **SquadBattle 矛盾文案**：删 SquadBattleView「每日最多挑战10次」整行（与「无次数限制」矛盾、代码侧限制已删）；③ **CardDetailModal Pin 满文案**：把以「当前已 pin 数」表上限改用导出常量 `MAX_PINS`（语义错位、凑巧相等）；④ **NurtureActions setTimeout 登记**：两处裸 setTimeout（3s 动画 + 最长 60min 训练播报）未登记，仿 SquadBattleView 的 `schedule()`（登记进集合 + onUnmounted forEach clearTimeout）收口——现有 onUnmounted 只清 setInterval。**坑接受声明**：60min 训练播报卸载清除后离开养成页不再 fire，是体验性日志非状态变更，可接受。
  - 验收：CLAUDE.md 与 pitfalls.md 版本号与 schema.ts:30（v12）一致、无残留 v11/v6；SquadBattle 矛盾文案删除（不再出现"每日最多挑战10次"与"无次数限制"并存）；CardDetailModal Pin 满文案改用 MAX_PINS 常量；NurtureActions 两处 setTimeout 登记并在卸载时清除（无裸 setTimeout 漏网）；既有行为不回归；**零存档**；type-check / test / build 通过。

**第 5 轮验收命令**：`cd frontend-vue && npm run type-check`（0 错）/ `npm run test`（≥489 全绿 + 新增，不弱化既有）/ `npm run build`（通过）；回归基线 `python backend/test_security.py`（全 PASS、退出码 0）/ `grep -rn "debug=True" backend/server.py api/index.py`（零命中）。**零存档铁律**：SAVE_VERSION 仍 12、schema/migrations/装配器三处无改动。

---
*创建于 2026-06-16，由 `/goal` 把 FUTURE.md 剩余任务拆分而来。活动 sprint 完成后更新 FUTURE.md 对应勾选与进度总览。*
